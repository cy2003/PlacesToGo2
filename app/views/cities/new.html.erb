<h2>Create new state, city, or town for <%= @city.country.name %></h2>

<%= form_for(@city) do |f| %>
 	<%= f.label :name %>
 	<%= f.text_field :name %>
  <br>
  <br>
  <%= f.hidden_field :country_id, :value => @city.country.id %>
 	<%= f.submit "Create State, City or Town" %>
<% end %>
