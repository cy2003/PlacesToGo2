<h2>Create new location for <%= @location.country.name %></h2>

<%= form_for(@location) do |f| %>
 	<%= f.label :name %>
 	<%= f.text_field :name %>
  <br>
  <br>
  <%= f.hidden_field :country_id, :value => @location.country.id %>
 	<%= f.submit "Create Location" %>
<% end %>
