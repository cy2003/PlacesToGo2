
<h3><%= @title %></h3>
<% if @users.any? %>
  <ul class="users follow">
    <% @users.each do |user| %>
      <li>
        <%= link_to "#{user.name}", user_path(user.id)%>
      </li>
    <% end %>
  </ul>
<% end %>
